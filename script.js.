import React, { useState, useEffect, useMemo } from 'react';
import { 
  ShoppingBag, 
  Music, 
  ExternalLink, 
  Play, 
  Instagram, 
  Youtube, 
  Globe, 
  Menu, 
  X, 
  ChevronRight,
  TrendingUp,
  Package,
  Mic2,
  Lock,
  Calendar,
  DollarSign,
  Target,
  Video,
  Search,
  CheckCircle,
  Plus,
  Minus,
  Trash2,
  ArrowRight,
  Star,
  Activity,
  Film,
  Cpu,
  Truck
} from 'lucide-react';

const App = () => {
  const [activeTab, setActiveTab] = useState('home');
  const [cart, setCart] = useState([]);
  const [isMenuOpen, setIsMenuOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);
  const [searchQuery, setSearchQuery] = useState('');
  const [selectedCategory, setSelectedCategory] = useState('All');
  const [checkoutStep, setCheckoutStep] = useState(0); 

  const APP_NAME = "V3GASBOI";
  
  const assets = {
    hero: "c98fa7b3-0dbd-4187-a5d0-05c32bc28c2d-1_all_1564.png",
    profile: "c98fa7b3-0dbd-4187-a5d0-05c32bc28c2d-1_all_1845.jpg",
    selfie: "c98fa7b3-0dbd-4187-a5d0-05c32bc28c2d-1_all_3403.jpg",
    pose: "c98fa7b3-0dbd-4187-a5d0-05c32bc28c2d-1_all_1572.png",
    covers: {
      good: "c98fa7b3-0dbd-4187-a5d0-05c32bc28c2d-1_all_1599.png",
      crazy: "c98fa7b3-0dbd-4187-a5d0-05c32bc28c2d-1_all_1604.png",
      space: "c98fa7b3-0dbd-4187-a5d0-05c32bc28c2d-1_all_1610.png",
      money: "c98fa7b3-0dbd-4187-a5d0-05c32bc28c2d-1_all_1629.jpg",
      wait: "1000010906.png"
    },
    event: "c98fa7b3-0dbd-4187-a5d0-05c32bc28c2d-1_all_1806.jpg"
  };

  const officialLinks = {
    soundcloud: "https://on.soundcloud.com/3d76nCYWqzJVAGMM6",
    githubHub: "https://jonas88evans.github.io/V3GASBOI-HUB-or-Official-V3GASBOI",
    spotify: "https://open.spotify.com/artist/6LtPbqwAXeYEq7Bs7gvAlp?si=lO7QmlP5QoWWw2gdpQPu6A&pi=U8JHuDKnRZ6zS",
    apple: "https://music.apple.com/us/artist/vegasboi/1762524945",
    youtube: "https://www.youtube.com/@vegasboi2147",
    instagram: "https://www.instagram.com/vegasboii702?igsh=ZHIyNGZuMTZldzk=",
    tiktok: "https://www.tiktok.com/@v3gasboii",
    canva: "https://v3gasboii.my.canva.site/v3gasboi",
    replitPortal: "https://replit.com/@jonas88evans/Vegas-Boi-Portal"
  };

  const products = [
    { id: 1, name: "Independence Code: That Good Good", price: 19.98, type: "Album", category: "Digital", image: assets.covers.good, stock: "Full Master", rating: 5 },
    { id: 2, name: "Money I Gotta Get It!", price: 1.29, type: "Single", category: "Digital", image: assets.covers.money, stock: "Instant", rating: 4.9 },
    { id: 3, name: "I Need My Space - Studio Pack", price: 45.00, type: "WAV Pack", category: "Services", image: assets.covers.space, stock: "Premium", rating: 5 },
    { id: 4, name: "88MOB Legacy Signature Apparel", price: 35.00, type: "Merch", category: "Physical", image: assets.pose, stock: "Limited", rating: 5 }
  ];

  const filteredProducts = useMemo(() => {
    return products.filter(p => 
      (selectedCategory === 'All' || p.category === selectedCategory) &&
      p.name.toLowerCase().includes(searchQuery.toLowerCase())
    );
  }, [selectedCategory, searchQuery]);

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 50);
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  const addToCart = (product) => {
    setCart(prev => {
      const existing = prev.find(item => item.id === product.id);
      if (existing) {
        return prev.map(item => item.id === product.id ? { ...item, qty: item.qty + 1 } : item);
      }
      return [...prev, { ...product, qty: 1 }];
    });
  };

  const updateQty = (id, delta) => {
    setCart(prev => prev.map(item => {
      if (item.id === id) {
        const newQty = Math.max(0, item.qty + delta);
        return { ...item, qty: newQty };
      }
      return item;
    }).filter(item => item.qty > 0));
  };

  const cartTotal = cart.reduce((acc, item) => acc + (item.price * item.qty), 0);
  const cartCount = cart.reduce((acc, item) => acc + item.qty, 0);

  // Styling Constants
  const heavyShadow = "drop-shadow-[0_4px_4px_rgba(0,0,0,1)]";
  const neonText = "drop-shadow-[0_0_8px_rgba(245,158,11,0.6)]";
  const cardGlow = "shadow-[0_20px_50px_rgba(0,0,0,0.8)]";

  return (
    <div className="min-h-screen bg-[#020202] text-white font-sans selection:bg-amber-500 selection:text-black overflow-x-hidden">
      {/* CEO Mission Bar */}
      <div className="fixed top-0 w-full z-[60] bg-black/90 backdrop-blur-md border-b border-white/5 py-2 px-6 flex items-center justify-between">
        <div className="flex items-center gap-3">
          <Activity className="w-3 h-3 text-amber-500 animate-pulse" />
          <span className={`text-[9px] font-black uppercase tracking-[0.3em] text-amber-500 ${neonText}`}>Mission: 10,000 Plays (92% Verified)</span>
        </div>
        <div className="flex items-center gap-4">
           <div className="w-24 h-1 bg-neutral-900 rounded-full overflow-hidden border border-white/5">
              <div className="h-full bg-amber-500 w-[92%] shadow-[0_0_10px_#f59e0b]"></div>
           </div>
        </div>
      </div>

      {/* Header */}
      <header className={`fixed top-8 w-full z-50 transition-all duration-500 ${scrolled ? 'bg-black/95 backdrop-blur-md py-3 border-b border-white/5 shadow-2xl' : 'bg-transparent py-8'}`}>
        <div className="max-w-7xl mx-auto px-6 flex justify-between items-center">
          <div className="flex items-center gap-4 cursor-pointer group" onClick={() => setActiveTab('home')}>
             <div className="w-12 h-12 bg-amber-500 rounded-sm flex items-center justify-center font-black text-black text-2xl italic skew-x-[-15deg] group-hover:skew-x-0 transition-all shadow-[0_0_20px_rgba(245,158,11,0.5)]">V3</div>
             <div className="flex flex-col">
                <span className={`text-2xl font-black italic tracking-tighter leading-none ${heavyShadow}`}>{APP_NAME}</span>
                <span className="text-[8px] font-black tracking-[0.5em] text-amber-500 mt-1 uppercase">CEO Vegas Spot LLC</span>
             </div>
          </div>

          <nav className="hidden lg:flex gap-12">
            {['home', 'vault', 'visuals', 'hustle'].map(id => (
              <button 
                key={id}
                onClick={() => {
                  if (id === 'visuals') {
                    document.getElementById('visuals-portal')?.scrollIntoView({ behavior: 'smooth' });
                  } else {
                    setActiveTab(id === 'vault' ? 'store' : id);
                  }
                }}
                className={`uppercase text-[11px] font-black tracking-[0.4em] transition-all relative py-2 ${activeTab === (id === 'vault' ? 'store' : id) ? 'text-amber-500' : 'text-neutral-500 hover:text-white'}`}
              >
                {id}
                {activeTab === (id === 'vault' ? 'store' : id) && <span className="absolute bottom-0 left-0 w-full h-0.5 bg-amber-500 shadow-[0_0_10px_#f59e0b]"></span>}
              </button>
            ))}
          </nav>

          <div className="flex items-center gap-6">
            <button 
              onClick={() => setActiveTab('cart')}
              className="relative p-3 bg-white/5 border border-white/10 rounded-full hover:bg-amber-500 hover:text-black transition-all shadow-xl"
            >
              <ShoppingBag className="w-6 h-6" />
              {cartCount > 0 && (
                <span className="absolute -top-1 -right-1 bg-amber-500 text-black text-[10px] font-black w-5 h-5 rounded-full flex items-center justify-center border-2 border-black">
                  {cartCount}
                </span>
              )}
            </button>
            <button className="lg:hidden" onClick={() => setIsMenuOpen(true)}><Menu/></button>
          </div>
        </div>
      </header>

      {/* Main Content */}
      <main className="pt-24">
        {activeTab === 'home' && (
          <section className="animate-in fade-in duration-1000">
            {/* Hero */}
            <div className="relative h-[85vh] flex items-center overflow-hidden">
               <div className="absolute inset-0">
                  <img src={assets.hero} className="absolute bottom-0 right-[-5%] h-[115%] w-auto object-contain object-bottom opacity-70 grayscale hover:grayscale-0 transition-all duration-[2s]" alt="V3 Hero"/>
                  <div className="absolute inset-0 bg-gradient-to-t from-[#020202] via-[#020202]/20 to-transparent" />
                  <div className="absolute inset-0 bg-gradient-to-r from-[#020202] via-transparent to-transparent" />
               </div>

               <div className="relative z-10 max-w-7xl mx-auto px-8 w-full text-left">
                  <div className="max-w-2xl">
                     <div className="flex items-center gap-4 mb-8">
                        <span className="h-px w-10 bg-amber-500"></span>
                        <span className="text-amber-500 font-black tracking-[0.5em] text-[10px] uppercase italic">The Sound & Movement</span>
                     </div>
                     <h1 className={`text-[5rem] md:text-[10rem] font-black italic leading-[0.85] tracking-tighter mb-8 ${heavyShadow} ${neonText}`}>
                       VOICE<br/>OF VEGAS.
                     </h1>
                     <p className={`text-xl md:text-2xl text-neutral-300 font-medium mb-12 max-w-lg border-l-4 border-amber-500 pl-8 leading-relaxed italic ${heavyShadow}`}>
                        "The multi-hyphenate mogul bridging digital content and global e-commerce."
                     </p>
                     <div className="flex flex-wrap gap-6">
                        <a href={officialLinks.soundcloud} target="_blank" rel="noopener noreferrer" className="group px-12 py-5 bg-amber-500 text-black font-black uppercase tracking-widest text-xs hover:bg-white transition-all transform hover:scale-105 flex items-center gap-4 shadow-[0_0_40px_rgba(245,158,11,0.5)]">
                           Latest Single <Play className="w-4 h-4 fill-current" />
                        </a>
                        <a href={officialLinks.githubHub} target="_blank" rel="noopener noreferrer" className="px-12 py-5 bg-white/5 border border-white/10 text-white font-black uppercase tracking-widest text-xs hover:bg-white/10 transition-all flex items-center gap-4">
                           V3GASBOI HUB <Globe className="w-4 h-4" />
                        </a>
                     </div>
                  </div>
               </div>
            </div>

            {/* Marquee */}
            <div className="bg-amber-500 py-4 border-y-2 border-black rotate-[-1deg] translate-y-[-20px] shadow-2xl overflow-hidden relative z-20">
               <div className="flex animate-marquee gap-14 font-black text-black uppercase italic tracking-[0.25em] text-sm whitespace-nowrap">
                  <span>Vegas Spot LLC • 88MOB Movement • Independence Code • World of Fashions • Vegas Spot LLC • 88MOB Movement • Independence Code • </span>
                  <span>Vegas Spot LLC • 88MOB Movement • Independence Code • World of Fashions • Vegas Spot LLC • 88MOB Movement • Independence Code • </span>
               </div>
            </div>

            {/* Executive Bio Section */}
            <div className="py-32 max-w-7xl mx-auto px-8">
               <div className="grid lg:grid-cols-2 gap-24 items-center">
                  <div className={`relative ${cardGlow} rounded-[3rem] overflow-hidden border border-white/5 group`}>
                     <img src={assets.profile} className="w-full h-auto grayscale transition-all duration-1000 group-hover:grayscale-0 group-hover:scale-105" alt="CEO Profile"/>
                     <div className="absolute inset-0 bg-gradient-to-t from-black via-transparent flex items-end p-10">
                        <div className="p-8 bg-black/60 backdrop-blur-xl border border-white/10 rounded-2xl w-full">
                           <h3 className="text-3xl font-black italic uppercase leading-none">CEO Status</h3>
                           <p className="text-amber-500 font-black text-[10px] uppercase tracking-widest mt-2">Vegas Spot LLC Corporate</p>
                        </div>
                     </div>
                  </div>
                  <div className="space-y-10 text-left">
                     <h2 className={`text-6xl font-black uppercase italic tracking-tighter ${heavyShadow} ${neonText}`}>THE HUSTLE</h2>
                     <p className="text-neutral-400 text-2xl font-medium leading-relaxed italic border-l-8 border-amber-500 pl-8">
                        V3GASBOI isn't just an artist; he is a multi-hyphenate entrepreneur. As the CEO of Vegas Spot LLC, he bridges the gap between digital content and e-commerce, operating brands like World of Fashions.
                     </p>
                     <p className="text-neutral-500 text-lg leading-relaxed">
                        Whether he is mastering tracks with AI tools or managing a global shipping operation, he embodies the "hustle in the shadows of the casinos". Representing street authority through ownership since 1998.
                     </p>
                     <div className="grid grid-cols-2 gap-6">
                        <div className="p-8 bg-neutral-900 border border-neutral-800 rounded-3xl hover:border-amber-500 transition-all group">
                           <Cpu className="text-amber-500 w-10 h-10 mb-4 group-hover:scale-110 transition-transform" />
                           <h4 className="font-black uppercase italic mb-1">AI Mastering</h4>
                           <p className="text-[10px] text-neutral-500 uppercase font-bold">Elite Sound Production</p>
                        </div>
                        <div className="p-8 bg-neutral-900 border border-neutral-800 rounded-3xl hover:border-amber-500 transition-all group">
                           <Truck className="text-amber-500 w-10 h-10 mb-4 group-hover:scale-110 transition-transform" />
                           <h4 className="font-black uppercase italic mb-1">Global Shipping</h4>
                           <p className="text-[10px] text-neutral-500 uppercase font-bold">World of Fashions Ops</p>
                        </div>
                     </div>
                  </div>
               </div>
            </div>

            {/* Visual Portal Integrated */}
            <div id="visuals-portal" className="py-32 bg-black border-y border-white/5 relative">
               <div className="max-w-7xl mx-auto px-8 text-left">
                  <h2 className={`text-7xl font-black uppercase italic tracking-tighter mb-16 leading-none ${heavyShadow} ${neonText}`}>VISUAL<br/>FEED</h2>
                  <div className="grid lg:grid-cols-3 gap-10">
                     <div className={`lg:col-span-2 aspect-video bg-neutral-900 rounded-[3rem] overflow-hidden border border-white/10 shadow-2xl ${cardGlow}`}>
                        <iframe 
                          width="100%" 
                          height="100%" 
                          src="https://www.youtube.com/embed?listType=user_uploads&list=vegasboi2147" 
                          title="Visual Feed" 
                          frameBorder="0" 
                          allowFullScreen
                          className="absolute inset-0 w-full h-full"
                        ></iframe>
                     </div>
                     <div className="space-y-6">
                        <div className={`bg-neutral-900 border border-white/5 rounded-3xl p-6 hover:border-amber-500 transition-all ${cardGlow} group cursor-pointer`}>
                           <div className="aspect-video rounded-2xl overflow-hidden mb-4 relative">
                              <img src={assets.selfie} className="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-700" alt="Cinematic"/>
                              <div className="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity bg-black/40">
                                 <Play className="text-amber-500 w-12 h-12 fill-current" />
                              </div>
                           </div>
                           <h4 className="font-black uppercase italic text-lg leading-none">88MOB Cinematics</h4>
                           <p className="text-[10px] text-neutral-500 font-bold uppercase tracking-widest mt-2 italic">Official Visual Footage</p>
                        </div>
                        <div className={`bg-neutral-900 border border-white/5 rounded-3xl p-6 hover:border-amber-500 transition-all ${cardGlow} group cursor-pointer`}>
                           <div className="aspect-video rounded-2xl overflow-hidden mb-4 relative">
                              <img src={assets.pose} className="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-700" alt="Vlog"/>
                              <div className="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity bg-black/40">
                                 <Film className="text-amber-500 w-12 h-12" />
                              </div>
                           </div>
                           <h4 className="font-black uppercase italic text-lg leading-none">Hustle Chronicles</h4>
                           <p className="text-[10px] text-neutral-500 font-bold uppercase tracking-widest mt-2 italic">Studio & Logistics Vlog</p>
                        </div>
                     </div>
                  </div>
               </div>
            </div>
          </section>
        )}

        {activeTab === 'store' && (
          <section className="py-12 max-w-7xl mx-auto px-8 animate-in slide-in-from-right-10 duration-700 text-left">
             <div className="flex flex-col md:flex-row justify-between items-end gap-10 mb-20 border-b border-white/5 pb-12">
                <div>
                   <h2 className={`text-7xl font-black uppercase italic tracking-tighter ${heavyShadow} ${neonText}`}>THE VAULT</h2>
                   <p className="text-neutral-500 font-black tracking-[0.4em] uppercase text-[10px] mt-4">Vegas Spot LLC Official Distribution</p>
                </div>
                <div className="w-full md:w-auto flex flex-col items-end gap-6">
                   <div className="relative group w-full shadow-2xl">
                      <Search className="absolute left-4 top-1/2 -translate-y-1/2 text-neutral-600 w-4 h-4" />
                      <input 
                        type="text" 
                        placeholder="Search Inventory..." 
                        className="bg-neutral-900 border border-neutral-800 rounded-full pl-12 pr-6 py-4 text-sm focus:outline-none focus:border-amber-500 w-full md:w-[320px] transition-all italic font-bold"
                        value={searchQuery}
                        onChange={(e) => setSearchQuery(e.target.value)}
                      />
                   </div>
                </div>
             </div>

             <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-12">
                {filteredProducts.map(product => (
                  <div key={product.id} className="group relative">
                     <div className={`relative aspect-square overflow-hidden bg-neutral-900 rounded-[2rem] mb-8 border border-white/5 transition-all group-hover:border-amber-500/50 shadow-2xl ${cardGlow}`}>
                        <img src={product.image} className="w-full h-full object-cover grayscale transition-all duration-1000 group-hover:grayscale-0 group-hover:scale-110" alt={product.name}/>
                        <div className="absolute inset-x-6 bottom-6 translate-y-10 opacity-0 group-hover:translate-y-0 group-hover:opacity-100 transition-all duration-500">
                           <button 
                             onC
